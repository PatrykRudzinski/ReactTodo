import React from 'react';
import TodoList from '../todos/TodoList';

const Tasks = () => {
    return (
        <div>
            <h1>There are your today's tasks:</h1>
            <TodoList/>
        </div>
    )
}

export default Tasks
